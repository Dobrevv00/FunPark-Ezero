/**
 * Режим „Състезание“ — докато ключът по-долу е true:
 *
 *  • началната страница показва секцията за състезанието на мястото на
 *    календара за резервации (`CompetitionCard` вместо `BookingCard`);
 *  • записванията за стандартни сесии са спрени: всеки бутон и линк, който
 *    отваря резервационния календар (`useBookingModal().open()`), вместо това
 *    води към страницата „Записване за състезания“.
 *
 * Нищо от резервационната система не е премахнато — календарът, модалът,
 * билетите и админ панелът остават непокътнати в кода.
 *
 * Изключване: смени на false, commit и push към main — Vercel деплойва сам
 * (~минута), резервациите се връщат и календарът пак излиза на началната.
 *
 * Нарочно ТВЪРД ключ в кода, а не CMS поле — по същата причина като
 * COMING_SOON_MODE: няма нови колони в базата, така че деплоят не зависи от
 * достъп до Vercel/Neon и не пипа схемата.
 */
export const COMPETITION_MODE = true;
