/**
 * Режим „Състезание“ — докато ключът по-долу е true, записванията за
 * стандартни сесии са спрени: всеки бутон и линк, който отваря резервационния
 * календар (`useBookingModal().open()` в `components/BookingModal.tsx`),
 * вместо това води към страницата „Записване за състезания“.
 *
 * ИЗКЛЮЧЕНИЕ: началната страница ("/") нарочно НЕ го зачита — `open()`
 * проверява текущия път и там винаги отваря истинския календар, независимо
 * от този флаг. Записването за състезания остава непроменено навсякъде
 * другаде (страници „Събития“ и „Записване за състезания“, бутоните за
 * резервация в контактите, рожденните дни, футъра и т.н.).
 *
 * Нищо от резервационната система не е премахнато — календарът, модалът,
 * билетите и админ панелът остават непокътнати в кода.
 *
 * Изключване навсякъде другаде: смени на false, commit и push към main —
 * Vercel деплойва сам (~минута) и резервациите на стандартни сесии се връщат.
 *
 * Нарочно ТВЪРД ключ в кода, а не CMS поле — по същата причина като
 * COMING_SOON_MODE: няма нови колони в базата, така че деплоят не зависи от
 * достъп до Vercel/Neon и не пипа схемата.
 */
export const COMPETITION_MODE = true;
